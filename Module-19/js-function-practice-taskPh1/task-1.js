/**Task-1
Take four parameters. Multiply the four numbers and then return the result */

function parameters(numb1, numb2, numb3, numb4) {
    let Multiply = numb1 * numb2 * numb3 * numb4;
    return Multiply;
}
let ans = parameters(2, 4, 5, 6);
console.log(ans);
